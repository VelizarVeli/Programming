@model List<Intillegio.Common.ViewModels.LastProjectsViewModel>
<div class="section-block">
    <div class="container">
        <div class="section-heading">
            <h4>Latest Projects</h4>
            <div class="section-heading-line-left"></div>
        </div>
        <div class="row mt-30">
            @foreach (var model1 in Model)
            {
                <div class="col-md-4 col-sm-6 col-12">
                    <div class="project-grid">
                        <div class="project-grid-img">
                            <img src="@model1.Image" alt="img">
                        </div>
                        <div class="project-grid-overlay">
                            <h4><a href="#">@model1.Name</a></h4>
                            <p>@model1].Category.Name</p>
                            <span>@model1.Stage.ToString()</span>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
